// Deprecated shim to keep old include paths working. The unified packet
// definitions now live in NetTypes.h so both MeshRadio and ReliableSender use
// the same structure layout.
#pragma once
#include "net/NetTypes.h"
